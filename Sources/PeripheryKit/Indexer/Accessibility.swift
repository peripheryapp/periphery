import Foundation

enum Accessibility: String {
    case `public` = "public"
    case `internal` = "internal"
    case `private` = "private"
    case `fileprivate` = "fileprivate"
    case `open` = "open"
}
